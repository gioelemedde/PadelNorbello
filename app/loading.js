function Loading() {
    return ( <div className="h-screen w-full flex justify-center items-center">
        <span className="loading loading-spiner w-[5rem] text-[var(--acc)]"></span>
    </div> );
}

export default Loading;